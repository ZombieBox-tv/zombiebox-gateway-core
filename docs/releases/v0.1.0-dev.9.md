# v0.1.0-dev.9: independent repository checkpoint

Date: 2026-09-20. Filtered history from the original dev.8 checkpoint; see
[history provenance](../history.md). This adds independent build/CI/docs ownership
and explicit shared-source dependencies. It does not complete a product milestone.

Go vet/race, live contracts and host wrapper/media smoke checks pass. Full images build from this core with explicit source contexts.

Formatting and dependency checks accompany this checkpoint. Historical tags are
under `monorepo/`; this new component tag points to the migration commit. All
remotes remain unconfigured and no hosted CI/publication is claimed. The workspace
records the exact compatible component commits and integration evidence.
